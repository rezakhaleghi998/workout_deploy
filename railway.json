{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "gunicorn fitness_tracker.wsgi:application --host 0.0.0.0 --port $PORT"
  }
}
